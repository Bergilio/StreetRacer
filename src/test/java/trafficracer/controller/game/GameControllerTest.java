package trafficracer.controller.game;

import trafficracer.model.game.road.Road;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;
/*
public class GameControllerTest {

    private Road roadMock;
    private TestableGameController controller;

    @BeforeEach
    void setUp() {
        roadMock = new Road(10, 20);
        controller = new TestableGameController(roadMock);
    }

    @Test
    void testGameController() {
        assertEquals(roadMock, controller.getModel(), "Pass");
    }

}

 */
