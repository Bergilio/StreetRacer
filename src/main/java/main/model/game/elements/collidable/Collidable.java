package main.model.game.elements.collidable;

import main.model.game.elements.Element;

public class Collidable extends Element {
    public Collidable(int x, int y, int w, int h) {
        super(x, y, w, h);
    }
}
